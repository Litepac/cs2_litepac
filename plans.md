# plans.md

Planning note:
- `In Progress` means actively being worked now, not historically related.
- Keep one active diagnosis/execution path in `In Progress` unless parallel work is genuinely happening right now.
- Paused or later work stays in `Planned` under `On Hold / Later`.
- `Done` should reflect what actually landed, not superseded intermediate versions.

## Planned
### Next Up
- Execute the Replay V1 spec in `docs/14-replay-v1-product-spec.md` with the 2D replayer as the primary product surface: finish `Position Player`, strengthen live selected-player study, improve dense-fight readability, and sharpen utility-review-to-live jumps before spending more iteration budget on Stats polish
- Use `docs/15-domain-alpha-readiness.md` as the productization checkpoint before public hosting work: move parser/upload/library persistence toward a hosted service model, keep Replay as the hero surface, and treat local-browser storage plus machine-specific startup paths as development scaffolding rather than the public product architecture
- Replace the temporary extracted CS2 equipment silhouettes with repo-owned icons before any public/domain-facing release path
- Add parser-backed match played time only if a trustworthy demo source is verified; the current parser + `demoinfocs` path does not expose a clean wall-clock match-played timestamp, so Matches should stay explicit about that gap instead of guessing from file metadata
- Add fixture/integration coverage around parser-to-viewer ingest flow once the current product surfaces settle

### On Hold / Later
- Finish the replay bottom area with one tightly scoped geometry/readability pass so the dock feels unified, compact, and clearly separated from the round strip without regressing map space
- Polish the new match stats destination into a stronger premium analytical surface while keeping it table-first and limited to trustworthy parser-backed stats
- Tighten placement-aware CT/T role inference across the active map pool by refining one map at a time from staged fixture output, with Inferno, Overpass, Ancient, and Mirage now materially ahead of the rest and the remaining maps needing fixture-backed validation
- Productize the advanced stats tabs so the new data plumbing reads like a deliberate analysis product surface instead of raw debug tables, while keeping the current compact page structure
- Compress the advanced stats framing so the scoreboards start higher: one tighter analysis toolbar, fewer repeated explanatory bands, and shorter Roles / Style notes
- Calm the Stats scoreboard typography so player identity stays strongest, rating remains important but integrated, and secondary values stop competing equally
- Strip remaining pre-table promo chrome from Stats and reduce the stacked-card feeling so the route behaves more like one unified analysis surface
- Make the Summary table more ruthless by cutting secondary stats, keeping only first-read metrics, and softening the remaining grid so the data scans more naturally
- Continue moving Stats from "upgraded scoreboard" toward a real analysis product by exposing faster per-view takeaways before the tables
- Revisit parser fallback identity for players with null Steam IDs only if a real collapse/split case is reproduced from a demo; current synthetic `displayName + userID` fallback plus `SourceDemo.Notes` warnings look conservative enough for now
- Tighten parser edge cases found during visual replay checks
- Add canonical inferno footprint truth if future UI needs more than soft center-based fire rendering
- Add map calibration spot checks tied to known landmarks
- Reduce viewer bundle size and split heavy Pixi code paths
- Improve utility lifecycle fidelity after visual validation
- Tighten canonical replay fields where UI quality is blocked by missing trustworthy render data
- Reconcile intentional post-round sampling through `officialEndTick` with round/stream validation so parser truth stays internally consistent
- Make bomb timer/state fully parser-owned and event-backed so planted countdown truth no longer depends on fragile parse-end state or inference
- Consider a later Figma refinement loop for Home, Matches, shell consistency, and state polish once the product structure is stable enough for design refinement instead of active workflow churn
- Revisit a separate internal AI runtime surface only if direct VSCode/Codex integration becomes valuable enough to justify a true runtime-first sidecar instead of another staged prototype

## In Progress
- None.

## Done
### Recent Done
- Completed alpha readiness cleanup slice 1: retired inactive legacy Home / Matches V2 / old replay layout components from active source, removed the Matches shell upload DOM lookup in favor of a direct input ref, added `docs/16-alpha-qa-checklist.md`, updated stale docs references, and verified with viewer build plus parser tests.
- Completed the repo readiness audit pass for domain/alpha preparation: captured the key hosted-ingest, server-persistence, API-hardening, validation, bundle/CSS, and legacy-runtime findings in `docs/15-domain-alpha-readiness.md`; also fixed two small frontend risks by making replay shortcuts ignore focused interactive controls and giving the Feedback panel dialog-style focus/Escape behavior.
- Completed a small domain-readiness stabilization pass: parser health now reports explicit runtime mode, the local launcher uses `dev:local` with `dev:bridge` kept as an alias, the Cloudflare shortcut preflights localhost before starting the tunnel, runbook/README startup guidance now checks `mode`, and Matches rows no longer nest button controls inside a row-as-button surface.
- Refined the current replay timeline into a calmer unified rail system: the grey lane remains the primary timeline surface, elapsed progress is now a softer full-lane state instead of a separate red string, bomb phase stays full-height with better contrast, kill lines are quieter, utility remains pinned to the lower lane, and bomb icons/labels are slightly clearer.
- Changed the current replay timeline to use the full grey lane as the rail surface: elapsed progress now fills the lane height, bomb phase spans the same lane top-to-bottom, kill lines fill the lane height, and utility markers sit on the bottom edge like attached timing cues.
- Unified the current replay timeline rail proportions without changing the page: elapsed progress now fills the neutral rail at the same height instead of floating as a separate red line, kill markers are shorter and calmer, utility markers sit closer to the rail, and bomb icons/labels are slightly larger while the bomb phase stays behind the playhead.
- Refined the current replay timeline rail away from the thick red-slab read: the primary rail is now a slimmer neutral track with restrained red elapsed progress, utility stays attached in a lower CT/T-colored lane, bomb phase is a darker separate state behind the rail, and the playhead remains the strongest positional layer.
- Micro-polished the current replay timeline: kill markers are now thinner one-pixel CT/T-colored precision lines with reduced opacity, and the playhead handle no longer renders as a dark square while preserving the current dock, utility lane, and bomb/defuse icon behavior.
- Anchored the replay timeline event language to one stronger rail system: utility markers now use parser-derived thrower side only and hang from the lower edge of the rail, kill events remain tall CT/T-colored lines through the event field, and bomb/defuse icons stay as the only icon events while the bomb phase remains a muted state behind progress.
- Tightened the replay timeline dock proportions without changing layout: row height is reduced slightly while keeping the larger event lane, the left play/time block and right controls are narrower and less dominant, marker positions are anchored closer to the rail system, and the bomb label now sits inside the bomb phase instead of floating above it.
- Strengthened the current replay timeline dock hierarchy without adding new marker types: the timeline row now uses more vertical height, the primary rail is thicker and more deliberate, the event lane has a clearer framed field, kills/icons/utility are staged with more breathing room, and the bomb phase remains behind progress as a darker state.
- Refined replay timeline spacing/contrast only: the event lane now uses more vertical height, kill lines are taller, utility/bomb icons sit higher as secondary signals, and the bomb phase is a darker brown-maroon background state behind the rail so elapsed progress and the playhead stay readable inside post-plant time.
- Refined the simplified replay timeline without touching the map/HUD/rosters: kill markers now use parser-derived killer side to render tall CT/T-colored event lines, bomb plant/explode use the existing C4 icon, defuse uses the existing defuser icon, and utility remains secondary as small minimal marks on the same primary rail.
- Simplified the active replay timeline after the semantic-marker pass became too visually busy: the dock now returns to one primary rail with quiet elapsed progress, a subtle bomb-phase segment, thin kill marks, lightweight utility marks, and minimal bomb marks while preserving parser-owned event timing and the current map/HUD/roster layout.
- Rebuilt only the active replay timeline dock into clearer layers: the round selector, semantic event lane, neutral/red progress rail, bomb-phase window, playhead, transport, and context row now read as separate timeline information instead of one crowded strip; marker semantics come from existing parser-owned `bombType` / `utilityKind` / kill data, and the viewer build passes.
- Added a real replay map drawing toolbar and shortcuts: Move mode (`M`) keeps pan/zoom behavior, Drawing mode (`D`) captures red annotation strokes on the replay stage, Clear drawing (`C`) removes them, and Space toggles play/pause while ignoring editable inputs.
- Tightened the replay readability scale after the previous pass overshot: roster cards now sit at a 1080p-safer middle size while preserving weapon/equipment/utility detail, and the top HUD is narrowed from the wide banner treatment by reducing team block width, padding, and team/alive spacing.
- Made the replay readability pass visibly larger instead of micro-adjusted: HUD height increased modestly from 42px to 56px with materially larger team/timer/score/alive text, and both roster card systems increased from 92px to 118px minimum height with larger names, glyphs, metadata, and equipment/utility icons.
- Polished the current replay page without changing structure: compact HUD text now has stronger contrast, larger team/timer/score/alive hierarchy, and the timeline playback handle no longer renders the dark filled square artifact.
- Completed the strict replay correction pass without changing structure: side roster cards are materially larger and easier to read, the top replay HUD is narrower and shorter, and the timeline rail/fill/ruler/needle now share one aligned track coordinate system so the white handle sits on the active red progress position.
- Final-polished the current replay page without restarting it: weapon glyphs no longer fall back to an incorrect M4 icon, common weapon aliases such as Desert Eagle now map correctly, player cards are larger and show truthful active/primary weapon plus armor/helmet/utility icons, the top HUD is narrower, and the timeline rail/markers/needle were refined for a more deliberate premium read.
- Tightened replay readability without changing the map-first structure: round chips and timeline labels are larger/sharper, the timeline rail has a clearer continuous base/progress/needle relationship, the top HUD is more compact, and side roster panels now size around larger readable player cards instead of stretching a mostly empty full-height column.
- Corrected the current replay page without restarting it: Home and Matches now live in the left DemoRead rail and exit replay cleanly, the top HUD is shorter, roster cards are denser and less stretched, round chips are larger with subtler active red treatment while preserving CT/T win color, and the timeline track reads as a cleaner continuous playback rail.
- Polished the current map-first replay page without restarting it: the top HUD now has stronger timer/score/team hierarchy, compact roster cards are lighter and clearer, the bottom dock has more deliberate round/playback/timeline separation, and the parser-owned timeline markers/ruler/needle are larger and easier to scan.
- Corrected the active replay page toward the agreed Skybox-style information hierarchy without restarting it: player panels now use compact icon-backed cards with weapon/utility/money/HP/armor context, the left rail uses the proper DemoRead logo asset, the top HUD is a tighter team/timer/score/round/mode structure, analysis filters are separated from the timing dock, and the timeline has clearer ruler labels, markers, and current-position needle
- Refined the active map-first replay page without reverting the reset: restored a stronger replay HUD with map/round/mode/team/score/timer/alive context, moved Live / Utility / Paths / Player / Heatmap into a clean left mode rail, split live roster support back into left/right CT/T panels, and changed active round chips to use red selection accents while preserving CT/T winner color readability
- Cut the active replay route out of the old global replay shell: `App.tsx` now mounts `ReplayMapFirstPage` inside a dedicated `dr-replay-app` full-viewport wrapper instead of `sky-shell-replay` / `viewer-shell-replay`, so the new map-first page no longer inherits the legacy left rail, replay-shell grid, or old timeline styling while still reusing parser-backed map/playback/timeline state
- Replaced the active replay page shell with a separate new `ReplayMapFirstPage` component: existing replay hooks/state/data/callbacks still feed the map, rosters, analysis controls, and parser-owned timeline, but the visible structure is now a fresh `dr-mapfirst-*` full-canvas workspace; the old `ReplayReviewPage` shell was later retired from active source during alpha-readiness cleanup
- Refined the new `ReplayMapFirstPage` after live design review: removed the awkward horizontal roster ribbons from the lower map, replaced them with a quieter right-side real-data support rail, reduced HUD/dock opacity and sizing, and kept the map as the dominant canvas while preserving the same replay, timeline, and analysis callbacks
- Rebuilt the replay page composition around the map-first Skybox direction: the active replay route now uses a clean stage layout with CT/map/T columns in live mode, keeps the parser-backed map/killfeed as the main object, moves analysis modes and player/utility filters into the unified bottom dock with the round selector/timeline/playback controls, and keeps all real replay data and callbacks intact
- Corrected the replay redesign toward the Skybox-style reference after the first reset missed the target: removed the heavy segmented replay header from the active workspace, turned analysis controls into compact map chrome, moved live CT/T rosters into one quiet right-side support stack, reduced map framing, and kept the bottom dock calmer while preserving the same replay/timeline/analysis behavior
- Superseded the first replay workspace reset after live direction review showed it was too boxed and header-heavy; the useful retained pieces are the DR sidebar branding, the duplicate-play-control removal, and the parser-owned timeline/transport preservation
- Redesigned the replay workspace bottom dock into a stronger DemoRead review timeline: round navigation now has a clearer active-round identity, the parser-owned timeline track is larger with phase bands and stronger event/current-position hierarchy, and the lower rail separates readouts from playback/filter controls without changing replay truth or transport behavior
- Integrated Feedback into the shared top navigation utility group so shell pages now read `status -> Feedback -> primary CTA` and the old floating right-edge feedback trigger is gone
- Muted the ingest round-rack red treatment without changing behavior: locked/current/ready rounds and the progress rail now use a deeper, more restrained DemoRead red so the timeline still reads clearly but no longer overpowers the Matches surface
- Fixed the real missing round-by-round ingest progress source: the live UI was attached to the `node-fixtureparse` fallback bridge, which only emitted `roundsParsed: 0` and then a final replay; `fixtureparse` now has a `-progress-ndjson` mode and the fallback bridge now spawns it and forwards `roundsParsed / roundsTotal` events so both Go API and fallback paths can drive the round rack progressively
- Refined the ingest round rack into a clearer processing timeline: once a real total exists, all labeled rounds render immediately over a subtle progress rail, completed rounds lock red, the next round shows as the active processing square, future rounds stay dark, and the rail/progress label advance only from real `roundsIndexed / roundsTotal` state
- Fixed the ingest round-rack label visibility bug: active unknown-count ingest states now render provisional round numbers instead of empty labels, and the round chip CSS keeps labels above scan/current overlays with clearer contrast through active and ready states
- Bound ingest progress to real parser signals instead of cosmetic scanning: the Go ingest API now pre-counts total rounds with a lightweight parser pass, streams that total before canonical parsing, carries `roundsTotal` on each parser progress event, and the local launcher now prefers the Go parser API before falling back so local uploads can show true `x / total` round locking
- Refined the Matches ingest round rack into the primary product moment: the viewer now accepts early `roundsTotal` progress if the parser emits it, derives real totals from the replay result before validation when available, uses a shorter detecting-round-structure scan when totals are unknown, makes stage progression quieter, and gives the all-red ready state a longer confident hold
- Rebuilt the Matches demo ingest module into the DemoRead product language: parser health now identifies Go API vs fallback bridge and verifies the fallback parser executable is runnable, upload stays in the upload stage until the parser response is accepted, ingest errors stay inside a polished failed state, and round progress uses truthful red-square scanning/filled states without faking unavailable round counts
- Final-polished Matches CTA consistency: Open now uses the same restrained red-accent primary-action language as Upload Demo, Stats is quieter, primary hover/focus/active states match better, and the Feedback control aligns to the centered page rail instead of floating on the raw viewport edge
- Tightened the Matches top area further toward a compact workspace header: reduced page/header gaps, smaller title/subcopy scale, shorter status/metric/search controls, and changed Upload Demo from a heavy red block into a restrained red-accent primary CTA that still reads as the first action
- Polished the Matches top section into a more compact workspace header: reduced title scale and vertical spacing, tightened the header/status cards and search controls, and restyled Upload Demo as the primary red CTA with stronger label hierarchy, arrow cue, and clearer hover/focus behavior
- Restructured Matches so upload, current state, saved/visible/map/parser metrics now live in the top header area; the separate right-side support rail is gone, search/filter stays below the header, and the match library is again the full-width main workspace with validation fixtures as a secondary strip below
- Aligned Matches to the same centered content frame as Home: the active Matches page now uses the shared 1420px centered rail and responsive gutters while keeping the current library/support two-column workspace inside that centered parent
- Tightened the Matches workspace layout so the match library is more dominant and row layout is robust: widened the page rail, narrowed/lightened the support panel, gave score/time/actions explicit stable columns, moved the rail below earlier on laptop widths, and strengthened the primary Open button without changing real parser-backed behavior
- Refined the Matches redline reset into a stronger DemoRead workspace: the active Matches route now uses the same top-nav treatment and visible CTA shape as Home, moves upload/status/fixtures into an integrated right support rail, keeps the match library as the main working surface, and makes Open visually dominant over Stats/Delete while preserving real parser-backed behavior
- Restarted the active Matches page as a DemoRead redline product-library surface: the page now has an integrated header/upload/status area, unified search/map/status controls, dense parser-backed match rows, clearer primary Open action, preserved Stats/Delete/fixture/upload behavior, and responsive black/red/off-white styling aligned with the new Home page
- Restyled the Feedback widget to match the current DemoRead landing direction: the trigger and panel now use black/red/off-white sharp chrome, red focus/hover states, and no older green/blue console palette while preserving the existing feedback behavior
- Final-polished the Home landing page after the product-preview removal: moved Feedback from the bottom-right to a compact top-right floating slot below the nav, tightened two copy lines, and verified the page still builds cleanly across the existing responsive CSS
- Removed the Home landing Product Preview section and its unused preview/map CSS/data, tightened the global landing section gap, and kept the page flow focused on hero, value, workflow, audience, and final CTA
- Retuned the DemoRead hero logo timing so the lockup builds as one overlapping motion phrase: Demo begins while DR settles, Read begins before Demo fully settles, the tagline follows more softly, and the existing tracer-underlined final state plus square fill remain clean and minimal
- Simplified the DemoRead hero underline effect so one small tracer streak travels along the underline path, draws the persistent red line, disappears, then the timeline squares fill and the final lockup holds without extra flash/cut-through overlays
- Refined the DemoRead hero lockup motion so the red underline persists as part of the completed logo state, a separate fast tracer pass cuts through after `Read` lands, the tagline follows that impact, and the red square strip fills decisively before the final hold/reset
- Cleaned up the DemoRead hero logo animation timing: DR, Demo, Read, and tagline now reveal as separate non-overlapping asset layers, the tagline has a crisp impact pass, the red square strip fills across as one decisive timeline, and the full final lockup holds before reset
- Fixed the DemoRead hero logo animation so it assembles from the separate repo asset pieces instead of clipping or stacking a full logo image: `DR Icon.png`, `Demo.png`, `Read.png`, and `DemoRead_tagline.png` now reveal in sequence as one stable final lockup with no duplicate wordmark underneath
- Refined DemoRead hero logo animation timing so the sequence is DR -> Demo -> Read -> tagline plus timeline impact -> hold -> reset: Demo now arrives before Read begins, the tagline and red timeline squares snap in together with a restrained tactical jolt, and the loop keeps the existing clean brand-only layout
- Refined the DemoRead hero logo lockup animation hierarchy: DR icon resolves first, Demo appears next, Read lands slightly after with stronger emphasis, a restrained red sweep passes across the wordmark, and a lower/smaller/wider-tracked tagline fades in before the timeline ticks
- Refined the Home hero copy and minimal brand-animation timing: headline now reads "See the round clearly.", subcopy uses the focused local-demo-to-2D-review wording, and the bottom timeline squares now fill faster, stronger, hold longer, and fade closer to loop reset without adding new visual elements
- Simplified the Home hero right-side motion into a pure DemoRead brand animation: removed background map/frame/path/marker/tactical geometry, split the transparent logo asset into staged CSS clip reveals for DR icon, wordmark, and tagline, then added only bottom timeline-square activation with reduced-motion fallback
- Replaced the Home hero's right side with a clean DemoRead brand-motion identity piece instead of a fake replay box: the active hero now floats the transparent logo over sharp grid/frame traces, subtle marker movement, a tactical cue, and timeline pulses, with no internal slogan, fake score, fake match card, or mini-dashboard structure
- Refined the Home hero motion piece from abstract route drawing into a miniature branded 2D replay moment: one T and two CT player markers now move along short readable paths, leave restrained trails, reveal utility and bomb cues, advance the timeline, and reset cleanly using CSS/SVG only with no demo/replay/parser dependency
- Replaced the Home hero's right-side match-card/product-proof box with a branded DemoRead motion piece: the active hero now uses the transparent logo, crisp grid/frame draw-in, replay-style routes, CT/T/bomb markers, utility/bomb labels, timeline activation, looped CSS/SVG motion, and reduced-motion fallback without demo/replay/parser loading or fake match/scoreboard content in the hero
- Reset the Home hero/top-of-page after subagent research and repo inspection: the hero now leads with clearer user value, uses an instant static product-proof artifact with map/timeline/path/round-result/context cues, removes arbitrary fake match names and implementation-note language from the public surface, and keeps Home free of replay/demo/parser loading with reduced-motion-safe CSS
- Replaced the Home hero preview box with a lightweight static-data motion piece: the loop uses the transparent DemoRead PNG, CSS/SVG animation for brand reveal, map frame, player markers, path lines, round score, context cue, and timeline strip, plus a reduced-motion static final-frame fallback; no replay/demo/parser loading is involved
- Cleaned up the final DemoRead logo/hero presentation: cropped the transparent PNG to the actual mark, removed remaining opaque dark-neutral background pixels, switched navbar sizing to height-based aspect-preserving rendering, and removed the meaningless hero map filler polygon so the static preview is only grid/routes/tokens/context/timeline
- Switched the public brand rendering to the repo-provided `assets/DemoRead_Logo.png`: the top nav now uses the PNG with alt text, the generated SVG logo component/CSS is removed from active source, and the Home hero stays a static product mock instead of a logo billboard
- Refined the static Home landing hero after the DemoRead logo pass: the navbar logo stays, the right hero panel is now a product-derived static map/timeline/context mock instead of a large wordmark placeholder, and public copy no longer exposes implementation notes like no-demo-load or replay-opening mechanics
- Added a reusable code-native DemoRead SVG logo system: angular DR monogram, off-white/red wordmark, icon-only/wordmark/full variants, nav/hero/app-icon sizes, optional "Review what matters" tagline, navbar integration, and hero brand-panel usage without raster assets or generated images
- Replaced the Home hero's right-side replay-style preview with a fully static Demoread brand/product panel, changed the Home nav status away from parser/live bridge language, and gated replay-loader parser/library effects off while `shellPage === "home"`, so the landing route presents as an instant brand/product surface while real replay/demo loading stays in Matches/replay pages
- Converted the current Home landing hero/Product Preview to a lightweight static product visual: removed latest-match replay data, map image loading, parser-state preview dependency, and direct latest-replay CTA behavior from `HomePage`, so the homepage can paint instantly while real demo/replay loading stays behind Matches/replay routes
- Final-polished the Home landing page without redesigning it: tightened the hero around the more memorable "See the round clearly" line, made Product Preview copy more natural, added restrained lower-page composition accents, and sharpened the alpha CTA finish while keeping the no-blur Redline grid direction
- Softened the Home landing typography and public copy without changing the structure: replaced the awkward "scrubbing blindly" headline with clearer review-focused language, warmed the white text, reduced hero scale/weight, and made later section headings calmer than the hero
- Refined the current Home landing page without changing the route structure: the hero preview now has stronger replay-workspace proof from the latest local match, the Product Preview section includes a concrete match/library/map/context surface using replay-derived counts, the lower half is tighter, and the sharp grid background remains free of blur/radial glow in the active landing block
- Removed the remaining soft/glowy Home background treatment: the active Home shell now uses hard-edged near-black grid/base layers, the landing route disables nav blur, the landing pseudo-element SVG filter/noise layer is gone, and the final CTA/preview chrome uses crisp lines instead of large fading red gradients
- Refined the benefit-first Home landing page without rebuilding the structure: upgraded the hero into one polished replay-window preview with map, context rail, and timeline; replaced soft/glowy background gradients with a sharper near-black grid/edge treatment; shortened mid-page copy; and strengthened the final alpha CTA
- Reset the default Home route into a simpler one-page landing page: the headline now leads with user value instead of internal parser wording, product proof is reduced to one polished preview plus concise feature rows, and the older Home / Matches V2 route experiments were later retired from active source during alpha-readiness cleanup
- Superseded the first Redline Home pass after review showed it was too dashboard-heavy and technical; the useful routing cleanup remains, but the default Home has now been reset to the simpler benefit-first landing page above
- Narrowed `Position Player` around the sharper product job: selected-player timing/pattern study is now the primary workflow in the panel copy/state flow, while broad all-player compare is an explicit secondary action instead of the implicit default fallback
- Shipped the recent replay trust/readability pass: live nametags now use full bottom-locked names, death markers persist quietly for the whole round, dropped-bomb reconciliation improved materially, the replay HUD now uses parser-backed timer/alive-count semantics in a three-box layout, and stale local-clock fallback behavior was removed instead of guessed
- Reworked replay shell structure enough to establish the current baseline: live CT/T rosters are split onto opposite sides of the map, non-live Utility / Paths / Player / Heatmap selectors now share one-row full-name team strips on their own layout path, and utility controls were pulled back to one row instead of the denser but weaker stacked experiment
- Audited parser fallback identity for null Steam IDs and decided not to treat it as active bug work unless a real demo reproduces a collapse/split failure; current synthetic `displayName + userID` fallback plus `SourceDemo.Notes` warnings are conservative enough for now
- Added `docs/15-domain-alpha-readiness.md` as the current productization checkpoint so hosted ingest, server-side persistence, and Replay-first alpha readiness have a stable written target before public hosting work
- Repaired localhost startup on this machine so there is one reliable local path again: restored the viewer `dev` script to Vite's native config loader after proving `runner` failed during config load, fixed the Windows `dev:bridge` wrapper so it can spawn `npm.cmd`, added the root-level `start-localhost.cmd` launcher, then refined that launcher to clear stale localhost temp logs and open the known-good path in its own console window; verified the stable path with `http://127.0.0.1:4173/` plus proxied `http://127.0.0.1:4173/api/health`
- Tightened repo docs and hygiene around the current workflow: reduced docs clutter, updated the runbook/README set, removed stale temp artifacts, and normalized local startup expectations so the repo reflects the real parser/runtime path again

### Historical Milestones
- Built the canonical replay foundation from parser truth outward: schema-backed replay output, parser CLI/HTTP ingest, staged fixtures, validation, compact replay emission, and parser module splits all landed without weakening the canonical replay contract
- Strengthened replay fidelity across parser and viewer with yaw sanitization, inferno handling, bomb/kill/hurt positional checks, official-end sampling continuity, and viewer interpolation behavior tied to parser truth rather than guesswork
- Built the local ingest and match-library flow: `.dem` upload, parser-backed progress reporting, persistent local matches, safer row identity, and a denser Matches surface with honest parser-backed semantics
- Shipped replay analysis as a real workspace over the normal replay timeline: `Utility Atlas`, `Positions`, `Heatmap`, and `Position Player` all run off canonical parser truth and were validated against staged fixtures instead of visual feel alone
- Rebuilt the replay workspace into a denser operator shell with stronger map dominance, tighter navigation/dock geometry, and a clearer analyst-console hierarchy while keeping parser-owned replay truth intact
- Added a real Stats destination and iterated it into a parser-backed analysis surface with conservative approximate metrics, compact round breakdowns, and placement-aware role notes without inventing unsupported replay truth
- Added durable repo guidance and regression support outside chat history: specialist guides, product-surface docs, advanced-stats docs, performance baselines, and the current runbook now capture repeatable rules and verification paths
- Built the local friend-testing path end to end with same-origin `/api` proxying, Cloudflare quick-tunnel compatibility, feedback intake, and usage telemetry so the product can be tested outside the dev machine before hosted infrastructure exists
- Removed the experimental internal AI operations surface after it proved too distracting and too weakly tied to the replay-first product direction

## Blocked
- Visual truth-checking still needs screenshots or side-by-side demo review
- Automated headless measurement of the replay dock's rendered height is blocked locally by browser permission failures, so exact before/after pixel verification still needs a working browser automation path or a live browser measurement hook

